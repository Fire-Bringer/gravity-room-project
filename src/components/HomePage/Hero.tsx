const Hero = () => {
  return (
    <div
      id="Home"
      className="bg-cover bg-center bg-no-repeat overflow-hidden"
      style={{
        backgroundImage: `url('images/earth-image.png')`,
        height: '100vh'
      }}
    />
  );
};

export default Hero;
